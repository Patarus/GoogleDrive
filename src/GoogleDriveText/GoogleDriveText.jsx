
import styles from '../GoogleDriveText/GoogleDriveText.module.scss';

const GoogleDriveText = () => {
    return (
        <span className={styles.textStyle}>Google Drive</span>
    );
}

export default GoogleDriveText;
